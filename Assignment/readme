# Vithusha Pathmanathan 
# Assignment REST API (Node.js + Express + MongoDB)

This project is a simple REST API built with Node.js, Express, and MongoDB. It supports:

- User Registration and Login (no authentication required)
- CRUD operations for Products (name, price, quantity)
